
## Layout
```
.
├── 📁 config             # Non-Nix config files  
├── ⚙️ default.nix        # Defines modules & system-level config  
├── 🏠 home-manager.nix   # User programs & configurations  
├── 📦 packages.nix       # Packages to install for Darwin  
├── 🔒 secrets.nix        # Age-encrypted secrets (agenix)  
```
